export default class RoleRepository {
  constructor() {}

  add(roleInstance) {
    return Promise.reject(Error('not implemented'));
  }

  getById(roleId) {
    return Promise.reject(Error('not implemented'));
  }

  getAll() {
    return Promise.reject(Error('not implemented'));
  }

  update(roleInstance) {
    return Promise.reject(Error('not implemented'));
  }

  delete(roleId) {
    return Promise.reject(Error('not implemented'));
  }
}
